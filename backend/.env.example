# Database (PostgreSQL) – same names as docker-compose db environment
POSTGRES_DB=nt2db
POSTGRES_USER=nt2user
POSTGRES_PASSWORD=change_me_in_production

# Django connection to DB (use localhost when running Django on host)
DB_HOST=localhost
DB_PORT=5432

# Django
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS (comma-separated origins for SPA, e.g. http://localhost:3000)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Optional: password for create_first_admin (otherwise interactive prompt)
# NT2_FIRST_ADMIN_PASSWORD=
