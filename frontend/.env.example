# Base URL for the API. Leave empty to use the Vite dev proxy (requests to /api go to http://localhost:8000).
# In production, set to the full API origin without trailing slash (e.g. https://api.example.com).
VITE_API_URL=
